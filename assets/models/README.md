# 3D Asset Model Folder

Drop production 3D files here when you have them.

- Place `.glb` or `.gltf` files in this folder for project organization.
- To make models available in the browser, copy or export them to `public/assets/models/`.
- The current hero loader expects `/assets/models/iphone.gltf`.
